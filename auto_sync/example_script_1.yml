# SYNC 1
- id: "example_sync"
  sync_type: "ldap"
  sync_args: "--process-groups --users mapped"
  sync_config:
    # Only need to put in what is DIFFERENT from the default files.
    # Note: dictionaries are recursed, but a non-dict values will be replaced
    # In theory, client can modify the ROOT files for unchanging parameters,
    # and then only a few entries would be needed in here.
    config:
      directory_users:
        groups:
          - directory_group: "All Sea of Carag"
            adobe_groups:
              - "Sea of Carag"
    connector:
      username: "adservice@seaofcarag.com"
      password: "P3rficient"
      base_dn: "DC=seaofcarag,DC=com"
      host: "ldap://seaofcarag.com:389"
    umapi:
      server:
        host: "usermanagement-stage.adobe.io"
        ims_host: "ims-na1-stg1.adobelogin.com"
      enterprise:
        org_id: "D28927675A9581A20A49412A@AdobeOrg"
        api_key: "30063bb75d2747faaaeaddaed6b36171"
        client_secret: "87e70dc7-1d76-4eaf-9b73-75366cf18458"
        tech_acct: "B7A639EA5B23D1D10A494022@techacct.adobe.com"
        priv_key_path:  "private.key"

# SYNC 2
- id: "example_sync2"
  sync_type: "ldap"
  sync_args: "--process-groups --users mapped"
  sync_config:
    # Only need to put in what is DIFFERENT from the default files.
    # Note: dictionaries are recursed, but a non-dict values will be replaced
    # In theory, client can modify the ROOT files for unchanging parameters,
    # and then only a few entries would be needed in here.
    config:
      directory_users:
        groups:
          - directory_group: "GROOP"
            adobe_groups:
              - "brian8A"
    connector:
      username: "adservice@seaofcarag.com"
      password: "P3rficient"
      base_dn: "DC=seaofcarag,DC=com"
      host: "ldap://seaofcarag.com:389"
    umapi:
      server:
        host: "usermanagement-stage.adobe.io"
        ims_host: "ims-na1-stg1.adobelogin.com"
      enterprise:
        org_id: "D28927675A9581A20A49412A@AdobeOrg"
        api_key: "30063bb75d2747faaaeaddaed6b36171"
        client_secret: "87e70dc7-1d76-4eaf-9b73-75366cf18458"
        tech_acct: "B7A639EA5B23D1D10A494022@techacct.adobe.com"
        priv_key_path:  "private.key"

# SYNC 3
- id: "example_sync3"
  sync_type: "ldap"
  sync_args: "--process-groups --users mapped"
  sync_config:
    # Only need to put in what is DIFFERENT from the default files.
    # Note: dictionaries are recursed, but a non-dict values will be replaced
    # In theory, client can modify the ROOT files for unchanging parameters,
    # and then only a few entries would be needed in here.
    config:
      directory_users:
        groups:
          - directory_group: "match-1"
            adobe_groups:
              - "fun1"
          - directory_group: "match-2"
            adobe_groups:
              - "fun2"
          - directory_group: "match-3"
            adobe_groups:
              - "fun3"
    connector:
      username: "adservice@seaofcarag.com"
      password: "P3rficient"
      base_dn: "DC=seaofcarag,DC=com"
      host: "ldap://seaofcarag.com:389"
    umapi:
      server:
        host: "usermanagement-stage.adobe.io"
        ims_host: "ims-na1-stg1.adobelogin.com"
      enterprise:
        org_id: "D28927675A9581A20A49412A@AdobeOrg"
        api_key: "30063bb75d2747faaaeaddaed6b36171"
        client_secret: "87e70dc7-1d76-4eaf-9b73-75366cf18458"
        tech_acct: "B7A639EA5B23D1D10A494022@techacct.adobe.com"
        priv_key_path:  "private.key"

# SYNC 4
- id: "example_sync4"
  sync_type: "ldap"
  sync_args: "--process-groups --users all"
  sync_config:
    # Only need to put in what is DIFFERENT from the default files.
    # Note: dictionaries are recursed, but a non-dict values will be replaced
    # In theory, client can modify the ROOT files for unchanging parameters,
    # and then only a few entries would be needed in here.
    config:
      directory_users:
        extension: "extension-config.yml"
        groups:
          - directory_group: "GROOP"
            adobe_groups:
              - "brian8A"
    connector:
      username: "adservice@seaofcarag.com"
      password: "P3rficient"
      base_dn: "DC=seaofcarag,DC=com"
      host: "ldap://seaofcarag.com:389"
    umapi:
      server:
        host: "usermanagement-stage.adobe.io"
        ims_host: "ims-na1-stg1.adobelogin.com"
      enterprise:
        # Test auto fill org_id from umapi file
        # org_id: "D28927675A9581A20A49412A@AdobeOrg"
        api_key: "30063bb75d2747faaaeaddaed6b36171"
        client_secret: "87e70dc7-1d76-4eaf-9b73-75366cf18458"
        tech_acct: "B7A639EA5B23D1D10A494022@techacct.adobe.com"
        priv_key_path:  "private.key"
    extension:
      after_mapping_hook: |
        import logging
        logger = logging.getLogger('ext')
        logger.info(str(sorted(source_groups)) + " " + str(sorted(target_groups)))
